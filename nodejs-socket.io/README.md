

```
yarn init -y
npx tsc --init
yarn add -D typescript
yarn add -D @types/express @types/node  tslint typescript
yarn add class-transformer class-validator dotenv express routing-controllers socket.io
```